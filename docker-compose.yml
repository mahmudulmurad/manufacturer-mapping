version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3111:3111"
    environment:
      NODE_ENV: development
    volumes:
      - .:/app
      - /app/node_modules
      - ./manufacturer.db:/app/manufacturer.db
    restart: always
